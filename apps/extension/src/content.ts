/**
 * Isolated-world content script bundled as a classic IIFE so Chrome can
 * inject it onto approved OSI origins. The page bridge is added in later
 * tasks; this file must exist in unpacked output.
 */
export {};
