coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: 'reach, diff, flags, files'

flag_management:
  default_rules:
    carryforward: true

flags:
  unit-nuxt:
    paths:
      - app/
      - server/
      - shared/
  e2e-api:
    paths:
      - app/
      - server/
      - shared/
