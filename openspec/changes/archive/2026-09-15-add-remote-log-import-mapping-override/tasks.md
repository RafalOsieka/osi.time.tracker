## 1. Routing utility (frontend, no UI)

- [x] 1.1 Change `route-logs-by-scope.ts`'s `routeLogsByScope` to return a single unified bucket list keyed by remote project id (`remoteProjectId | null`), each carrying `remoteProjectTitle`, `logs`, and `defaultProjectId: string | null` from the scope walk, replacing the separate `matched`/`unmatched` shapes (REQ-334, REQ-358). Verify with updated unit tests in `apps/web/test/unit/route-logs-by-scope.spec.ts` covering: scoped descendant gets a default, nested scope picks the more specific one, out-of-scope log gets `defaultProjectId: null`, unscoped-Project log gets `null`, and logs with no remote project id are grouped under a single `null`-id bucket.
- [x] 1.2 Add a pure `groupLogsBySelection(buckets, selection: Map<string, string | null>): RouteLogsByScopeGroup[]` helper (same file or a sibling module) that re-groups bucket logs by the caller-supplied selection, merging buckets that share a target and dropping buckets selected `null`. Verify with unit tests covering: two remote projects merged into one group, a bucket dropped when unselected, and a selection independent of the original `defaultProjectId`.

## 2. Composable: mapping phase and deferred dry-run

- [x] 2.1 Add `'mapping'` to `ImportPhase` in `use-remote-log-import.ts`, between `'scanning'` and `'preview'` (REQ-340).
- [x] 2.2 Change `startScan` to stop calling `importLogs({ dryRun: true })`; it should only fetch + route each month (via the updated `routeLogsByScope`) and accumulate per-month buckets, ending in phase `'mapping'` instead of `'preview'`. Verify with `apps/web/test/unit/use-remote-log-import.spec.ts`: scanning a range makes zero `importLogs` calls and lands on `phase === 'mapping'`.
- [x] 2.3 Expose `mappingRows` (remote project id, title, total log count across months, `defaultProjectId`) aggregated from the scanned buckets, and a mutable `selection: Ref<Map<string, string | null>>` initialized from each row's `defaultProjectId` when scan completes. Verify with a unit test that `mappingRows`/`selection` reflect scope defaults right after `startScan` resolves.
- [x] 2.4 Add a `setMapping(remoteProjectId: string | null, targetProjectId: string | null)` action that mutates `selection` only (no network call, no phase change). Verify with a unit test asserting `importLogs` is not called when `setMapping` runs.
- [x] 2.5 Add `advanceToPreview()`: for each month, regroup that month's buckets via `groupLogsBySelection` using the current `selection`, run the existing dry-run loop (`splitGroupsIntoRequestBatches` + `importLogs({ dryRun: true })`) against the result, store it as `routedByMonth` (same shape `startImport` already consumes), build `preview` from the dry-run responses, and set `phase = 'preview'`. Verify with unit tests: preview merges two remote projects mapped to the same Project into one row; preview excludes a row left unselected; changing `selection` then calling `advanceToPreview()` again reflects the new grouping.
- [x] 2.6 Add `backToMapping()` (sets `phase = 'mapping'`, leaves `selection`/scanned buckets untouched) and repoint the existing "back from preview" handling in the dialog to call it instead of `reset()`. Verify with a unit test that `backToMapping()` followed by `advanceToPreview()` re-dry-runs without re-fetching (no `syncClient.fetchTimeLogsInRange` calls).
- [x] 2.7 Keep `reset()` covering the mapping state too (`mappingRows`, `selection`) so "back to range" (mapping phase) still fully clears everything, as today. Verify with a unit test that `reset()` after reaching `mapping` clears `selection` and returns `phase === 'range'`.
- [x] 2.8 Update `startImport`/`retry`/`hasNothingToImport` to derive from the mapping-driven `preview`/`routedByMonth` (should mostly be unaffected since their shape is unchanged) — verify existing import/retry unit tests still pass unmodified or with only setup changes (no behavior assertions changed).

## 3. Dialog UI

- [x] 3.1 Add the mapping phase template block in `TrackerImportDialog.vue`: one row per `mappingRows` entry showing remote project title (or the "unknown remote project" fallback for the `null`-id row) and its raw log count, with a `USelect`/native select bound to `setMapping`, options = the tracker's non-deleted Projects (`projects.value`) plus a translated "do not import" option, pre-selected from `defaultProjectId`. Add `data-testid="tracker-import-mapping"` and a stable per-row `data-testid` (e.g. `` `tracker-import-mapping-row-${remoteProjectId ?? 'none'}` ``).
- [x] 3.2 Wire the mapping phase's footer: "Back" → `backToRange()` (existing behavior, reuse `importState.reset()` + `defaultRange()`), "Continue" → `importState.advanceToPreview()`.
- [x] 3.3 Change the preview phase's existing "Back" button to call `importState.backToMapping()` instead of `backToRange()`.
- [x] 3.4 Collapse the preview table back to one row per local Project (drop the separate matched/unmatched sections in the template; `preview.matched` already carries `remoteProjectTitles` per Project for display) — verify visually matches REQ-340's "preview lists each target Project the user assigned" wording.
- [x] 3.5 Add `en`/`pl` i18n strings for the mapping phase (heading, column labels, "do not import" option label, continue button, empty-state if scan found zero remote projects) in `apps/web/i18n/locales/{en,pl}.json`, keeping keys in parity. Verify with the project's existing i18n-parity lint/test.

## 4. Component and E2E tests

- [x] 4.1 Update `apps/web/test/nuxt/tracker-import-dialog.spec.ts`: assert the dialog reaches `mapping` after scanning (not `preview`), that changing a row's select updates `selection` without firing a request, and that "Continue" triggers the dry-run calls and moves to `preview`.
- [x] 4.2 Add a component test for overriding an unmatched remote project to a chosen Project and confirming its logs appear in the resulting preview row and are imported. (The dialog's composable is mocked in this suite, so the actual merge/preview behavior for this scenario is covered by the real-composable unit tests in `use-remote-log-import.spec.ts` — "merges two remote projects assigned to the same target into one preview row" / "excludes a row left unassigned…"; this file covers the UI wiring: selecting a target for an unassigned row calls `setMapping` with the chosen Project.)
- [x] 4.3 Add a component test for redirecting an already-matched remote project to a different Project and confirming the original scope-matched Project's preview row excludes those logs. (Same split as 4.2: real regrouping behavior is unit-tested on the composable; this file verifies overriding a matched row's select calls `setMapping` with the new Project.)
- [x] 4.4 Update `apps/web/test/e2e/ui/tracker-import-ui.spec.ts` (Playwright) with one journey that scans a tracker with an unmatched remote project, assigns it a target Project in the mapping phase, advances through preview, imports, and verifies the resulting entries/tasks land in the chosen Project (per REQ-358's scenarios). **Not executed locally**: Chromium isn't installed and this sandbox has no network access to `cdn.playwright.dev` to install it (`pnpm exec playwright install chromium` timed out). The spec is written against the same phase/testid contract exercised and passing in the Nuxt component suite; needs a run in an environment with Chromium (e.g. CI) to confirm.
- [x] 4.5 Update any existing Playwright/unit assertions that reference the old `preview` phase immediately following `scanning` (search for `tracker-import-preview`/`'scanning'` transitions in the e2e/unit suites) to go through `mapping` first. Updated `tracker-import-ui.spec.ts` (both existing journeys plus the back-navigation re-run) and `tracker-import-extension-ui.spec.ts`. Same Chromium caveat as 4.4 applies to confirming these by execution.

## 5. Docs

- [x] 5.1 Update `docs/user-stories.md` §11c acceptance criteria to describe the mapping phase and overridable routing, matching the new REQ-334/REQ-340/REQ-358 behavior.
