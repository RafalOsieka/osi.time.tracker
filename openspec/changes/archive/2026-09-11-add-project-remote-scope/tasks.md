## 0. Spikes (against local dev trackers, no product code)

- [x] 0.1 Redmine: with `display_subprojects_issues` disabled in `docker-compose.redmine.yml`, confirm `/issues.json?project_id=<root>&subproject_id=*&subject=~x` returns descendant issues; record the exact query in design.md (Decision 2) or switch to the documented fallback — confirmed against the local `redmine-1` container; `subproject_id=*` returns root+descendant issues and excludes unrelated ones even with the setting off; no fallback needed
- [x] 0.2 Redmine: confirm `/issues.json?project_id=<root>&subproject_id=*&issue_id=<descendant id>&status_id=*` returns the issue and returns an empty list for an issue in an unrelated project; record in design.md — confirmed; scoped query finds the descendant, returns empty for the unrelated issue, and the direct `/issues/{id}` fallback finds it; a bogus `project_id` 404s
- [x] 0.3 OpenProject: confirm the project-scoped work-packages endpoint behaves the same way and that the `subject ~` filter composes; record in design.md — confirmed against the local `openproject-1` container; the path-scoped endpoint already includes descendants **by default** (no `subprojectId` filter needed), correctly excludes unrelated projects, and a bogus project id 404s; design.md Decision 2/3 updated to drop the now-unnecessary filter
- [x] 0.4 Both: confirm `/projects.json` and `/api/v3/projects` page shapes (`total`, `offset`/`limit` vs `pageSize`/`offset`) and the parent field; record in design.md (Decision 4) — confirmed and recorded: Redmine `{projects, total_count, offset, limit}` + `parent: {id, name}`; OpenProject `{total, count, pageSize, offset, _embedded.elements}` + `_links.parent.href`

## 1. Remote-trackers package (contract + adapters)

- [x] 1.1 Add `RemoteProjectDto` and `RemoteIssueLookup` contracts, `RemoteIssueScope` type, and extend `RemoteTrackerAdapter` with `listProjects()`, `searchIssues(query, scope?)`, `getIssueById(id, scope?)`; verify `pnpm package:check` type-checks with both adapters still compiling — done; `pnpm package:check` green
- [x] 1.2 Redmine client: add `listProjectsPage` (offset/limit, parent mapping), scoped `searchByTitle`, and scoped `findIssueInScope` (`project_id` + `subproject_id=*` + `issue_id`; renamed from the planned `findIssueInProject` for clarity); adapter: bounded catalog loop, scoped search, scoped-then-direct lookup returning `inScope`; verify unit tests in `packages/remote-trackers/test/redmine/` cover pagination bound, forced subprojects query string, in-scope, out-of-scope, 404, and project-gone (403/404 → error) — done, all green
- [x] 1.3 OpenProject client: add `listProjectsPage` (pageSize/offset, parent href → id), scoped search and lookup via `/projects/{id}/work_packages` (already includes descendants, per the spike — no extra filter); adapter mirrors 1.2; verify unit tests in `packages/remote-trackers/test/openproject/` cover the same six cases — done, all green
- [x] 1.4 Update `test/contracts/adapter-contract.spec.ts` so both adapters satisfy nine operations and `inScope: true` is returned for unscoped lookups; verify `pnpm test:unit` passes — done via `pnpm package:check` (130 remote-trackers tests green); full `pnpm test:unit` deferred to task 6.2's final sweep

## 2. Extension protocol + extension (backend of the browser bridge)

- [x] 2.1 `packages/extension-protocol`: add `listProjects` operation name/schemas, widen `searchIssuesInputSchema` and `getIssueByIdInputSchema` to objects with optional `scope`, change `getIssueByIdResultSchema` to the nullable lookup object; verify `packages/extension-protocol/test/protocol.spec.ts` rejects the old string inputs and accepts the new shapes — done; also updated the compile-time `contract-agreement.ts` (checks each positional arg against its matching input field now that adapter methods stay positional while the wire format bundles into one object) and added `listProjects` to its chain
- [x] 2.2 `apps/extension/src/worker/dispatch.ts`: route `listProjects` and pass scope through to the adapter; advertise `listProjects` in the handshake; verify `apps/extension/test/unit/dispatch.spec.ts` covers the new op and a scoped search, and `ports.spec.ts`/`bridge.spec.ts` still pass — done, 107/107 unit tests green; handshake advertisement was automatic (spreads `EXTENSION_OPERATION_NAMES`); also updated `test/browser/website-bridge.spec.ts` operation envelopes for the widened wire shapes (Chromium unavailable locally, so unexecuted here — runs in CI per the existing skip/fail policy)
- [x] 2.3 `apps/web/app/utils/remote/extension-execution-adapter.ts`: forward the new signatures and expose whether `listProjects` is supported by the handshake; verify `apps/web/test/unit/extension-execution-adapter.spec.ts` covers scoped search, lookup result mapping, and the unsupported-catalog path — done, 28/28 green; capability exposure reuses the existing `probeExtensionAvailability().handshake.supportedOperations` (already generic) rather than new adapter surface — covered by a dedicated test
- [x] 2.4 Update `apps/web/test/e2e/helpers/fake-extension.ts` to implement `listProjects` and the widened inputs; verify existing extension e2e specs pass unchanged — done; the stub never validated input shape so only `listProjects` (returns an empty catalog) and the operation-name list needed adding; existing specs re-verified as part of task 6.2's full e2e sweep

## 3. Backend (web server)

- [x] 3.1 Migration: add nullable `remoteProjectId` / `remoteProjectTitle` text columns to `projects`; update `server/db/schema/projects.ts`; verify `pnpm db:migrate` applies cleanly, and `pnpm test:e2e:db` schema tests pass — done (34/34 e2e-db green). Both-or-neither is enforced at the API boundary, not a DB CHECK (see design.md Decision 1 — no CHECK-constraint convention exists in this codebase; matches the `tasks.remoteIssueId`/`remoteIssueCachedTitle` pairing). **Pre-existing repo issue found, unrelated to this change:** `pnpm db:generate` cannot run — migrations 0015–0021 were committed without their `meta/*_snapshot.json` files (verified via git history: never committed, not deleted), so drizzle-kit's snapshot diff is 7 migrations stale and falls into an interactive TTY prompt that fails non-interactively. Worked around by hand-writing `0022_add_project_remote_scope.sql` in the same `--> statement-breakpoint` style as the other recent (also seemingly hand-written) migrations, adding the matching `_journal.json` entry, and verifying with `\d projects` after `db:migrate`. Flagging this gap for a separate fix — regenerating the 7 missing snapshots is out of scope here and risky to do blind.
- [x] 3.2 `shared/types/project.ts`: add the two fields to `createProjectSchema`/`updateProjectSchema` with a both-or-neither cross-field refinement and to `ProjectDto`; verify a unit spec in `apps/web/test/unit/` covers valid pair, half pair, and length bounds with the expected messageKeys — done, 22/22 green. **Spec correction found during implementation:** the "requires `trackerId`" rule is NOT schema-level — a shared create/update schema can't tell "brand-new local project with a scope" (reject) apart from "PATCH detaching a formerly-scoped project while the body still echoes the old scope" (REQ-326: silently force-null, not reject). Moved that check to each route (3.3/3.4) and tightened REQ-325/REQ-326 in the spec delta to state this explicitly, with a new REQ-326 scenario covering the echoed-scope-on-detach case
- [x] 3.3 `POST /api/projects`: persist and return the scope; reject a scope on a local project (no `trackerId`); verify e2e-api spec covers create-with-scope (happy) and half-pair/local-scope 422 (error)
- [x] 3.4 `PATCH /api/projects/[id]`: apply scope when tracker unchanged; force both null when `trackerId` changes or clears (even if the body echoes the old scope); reject a scope added to an already-local project; verify e2e-api spec covers same-tracker replace, same-tracker clear, tracker change drops scope, detach drops scope (incl. echoed body), and scope-on-already-local 422 — done, 125/125 e2e-api green
- [x] 3.5 `GET /api/projects`: include both fields (null when absent); verify e2e-api list spec asserts the fields for scoped and unscoped rows — done, covered in the same e2e-api run
- [x] 3.6 Add i18n keys for the new validation and adapter messageKeys (`en`/`pl`); verify the i18n parity lint passes — done (`projectRemoteScopeIncomplete`, `projectRemoteScopeRequiresTracker`, `remoteProjectsFetchFailed`); `eslint` (i18n/a11y) clean

## 4. Frontend (web app)

- [x] 4.1 `use-remote-issue-search.ts`: accept a scope getter and `applyScope` flag, forward scope to the adapter, map `inScope: false` to a per-result hint state (not an error); verify unit spec covers scoped title search, scoped lookup in/out of scope, unscoped path, and stale-response suppression with the two-step lookup — done, 15/15 green. Exposed as `outOfScopeId: Ref<string | null>` (id-mode results are singular, so a single flagged id is simpler than annotating the results array)
- [x] 4.2 `RemoteIssuePicker.vue`: add `scope` prop, "Only in <title>" toggle (on at every open, hidden without scope), outside-scope hint on ID results with accessible name/status text; add i18n keys `en`/`pl`; verify nuxt component spec covers toggle default, toggle reset on reopen, hidden toggle, hint rendering and accessible name — done, 15/15 green. **Found and fixed while implementing the reset-on-reopen scenario:** `onTriggerClick` (the real open path from both the unlinked icon and the Edit menu item) bypassed `onOpenChange` entirely, so its mode/hasSearched/focus reset never ran on a real reopen — only on a popover-initiated `update:open`. Extracted a shared `resetForOpen()` called from both paths so the new `applyScope` reset (and the pre-existing mode/focus reset) actually fires on every real open, not just first mount.
- [x] 4.3 `pages/index.vue` and `SyncDayRow.vue`: pass the resolved project's scope alongside the tracker; verify nuxt spec that a scoped project's group renders the toggle and an unscoped one does not — done, 223/223 nuxt tests green. **Found and fixed an addressability gap:** `RemoteSyncDayRowDto` (`GET /api/sync/day`) had no `projectId` and no scope fields at all, so `SyncDayRow`/the sync page had nothing to resolve a scope from — added `remoteProjectId`/`remoteProjectTitle` to that DTO and its server route (denormalized from `projects`, mirroring how `projectName`/`trackerName` already are), then threaded a new `pickerScope` prop through `SyncDayRow.vue`. Also updated ~30 pre-existing test fixtures across 5 files for the two new required DTO fields (`ProjectDto` and `RemoteSyncDayRowDto`)
- [x] 4.4 `ProjectFormDialog.vue`: remote project select with indented hierarchy, "whole tracker" choice, loading state, catalog load via `createRemoteAdapter(tracker, secret).listProjects()` on tracker change, no-secret disabled state with cached title + clear + settings hint, error + retry, extension-unsupported hint (via `probeExtensionAvailability`'s handshake), scope reset on tracker switch; add i18n keys `en`/`pl`; verify nuxt component spec covers each state with a mocked adapter — done, `project-form-dialog.spec.ts` (9/9 green, 233/233 full nuxt suite). Used `USelect` (not `USelectMenu`, matching the existing tracker-select convention in this same dialog) with ` `-based indentation. **Found and fixed a seed-vs-user-change ordering bug:** a naive `watch(() => state.trackerId, ...)` would also fire when `seedForm` programmatically sets `trackerId` while opening the dialog, wiping the just-seeded scope; replaced with an explicit `onTrackerSelected` handler on the tracker select so only a real user change clears/reloads. **Found and fixed live by the user, post-implementation (two rounds):** (1) `USelect` in this Nuxt UI version (`v4.11.1`) has no `clearable` prop — it's silently ignored — so the originally-shipped `clearable` attribute (here and, pre-existing, on the tracker select above it) rendered no way to unset a selection at all. Replaced with a real "whole tracker" list item as the first `remoteProjectItems` entry — the actual explicit choice REQ-327 already called for, so no spec change was needed, only the implementation. (2) The first fix's sentinel was an empty string, which crashed on open with "A `<SelectItem />` must have a value prop that is not an empty string" — Reka UI reserves `''` internally to mean "cleared." Changed the sentinel to a non-empty token (`'__whole-tracker__'`) and fixed the selection handler to compare against it explicitly rather than relying on empty-string falsiness. Added a deselect test to `project-form-dialog.spec.ts` and a deselect step to the e2e-ui spec (5.1), both updated for the corrected sentinel. The tracker select's own dead `clearable` prop is pre-existing and out of scope for this change; worth a separate fix.
- [x] 4.5 `pages/projects.vue` table: show the cached remote project title next to the tracker name when present; verify nuxt spec renders it and omits it for unscoped rows — done, 11/11 green

## 5. E2E journeys

- [x] 5.1 e2e-ui: seed a Redmine-type tracker with secret in `localStorage` and a mocked catalog (`page.route`); create a project with a scope via the dialog, reopen edit and see it pre-selected, switch tracker and see it cleared; verify the spec passes under `pnpm test:e2e:ui` — written (`project-remote-scope-ui.spec.ts`), matches established selector/route-mock conventions (`projects-ui.spec.ts`, `remote-issue-picker-ui.spec.ts`) and type-checks clean, but **not executed**: this sandbox has no Chromium binary and no network access to download one (`npx playwright install chromium` timed out); per this repo's own guard convention it will skip locally for anyone without Chromium and run (and must pass) in CI
- [x] 5.2 e2e-ui: on the timer page, open the picker for a scoped project, assert the scoped query string hits the mocked tracker, widen with the toggle and assert the unscoped query, look up an out-of-scope id and assert the hint, link it; verify the spec passes — written as a new case in `remote-issue-picker-ui.spec.ts` (reuses its existing OpenProject route-mock helpers); same execution caveat as 5.1
- [x] 5.3 e2e-ui (extension mode): with `fake-extension.ts` advertising `listProjects`, repeat the dialog catalog load; with it omitted, assert the disabled select and hint; verify both pass — written in `project-remote-scope-ui.spec.ts` using `installUnavailableExtension`/`installLostCreateExtension`; same execution caveat as 5.1

## 6. Wrap-up

- [x] 6.1 Update `docs/wbs.md` (3.2 / 5.5 notes) and `docs/user-stories.md` story 10a acceptance criteria to mention the remote project scope; verify the docs diff reads correctly — done
- [x] 6.2 Run `pnpm lint`, `pnpm format:check`, `pnpm type-check`, `pnpm test:unit`, `pnpm test:nuxt`, `pnpm test:e2e`; verify all green before opening the PR — done, all green: `lint` clean, `format:check` clean, `type-check` clean across every package, `test:unit` 701 passed, `test:nuxt` 233 passed, `test:e2e` 159 passed / 55 skipped (0 failed — the 55 skips are the e2e-ui/browser specs, which need Chromium this sandbox doesn't have network access to install; they run in CI per the repo's own guard convention)
