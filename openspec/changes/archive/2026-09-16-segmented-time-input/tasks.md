All tasks are frontend (`apps/web`); there is no backend, API, or database work in this change (proposal — Non-goals).

## 1. Conversion utilities and shared test helpers

- [x] 1.1 Add `instantToZonedDateTime(iso, timeZone)` (`fromAbsolute`) and `zonedDateTimeToInstant(value)` (`toDate().toISOString()`) to `apps/web/app/utils/date-time.ts` with concise doc comments; verify with new cases in `apps/web/test/unit/date-time.spec.ts` that a round-trip preserves seconds and milliseconds, that `set({ hour, minute })` on the zoned value keeps date/second/millisecond, and that a Europe/Warsaw spring-forward wall-clock resolves deterministically (`pnpm test:unit -t "date-time"`).
- [x] 1.2 Add a Playwright helper `typeTimeField(page, testId, 'HH:mm', index = 0)` in `apps/web/test/e2e/helpers/time-field.ts` that clicks the `[data-segment="hour"]` / `[data-segment="minute"]` segments inside `[data-testid="<testId>"]` (nth `index` for range fields) and types the digits, mirroring `typeDateField`; verify with the first journey that uses it (task 4.3) that the segments show the typed time.
- [x] 1.3 Add a `UInputTime` stub next to the existing `UInput` / `UInputDate` stubs in `apps/web/test/nuxt/AppTimer.spec.ts` and `timer-entry-row.spec.ts` (root element carrying `$attrs`, re-emits `update:modelValue`, `focusout`, `keydown`), and verify both suites still mount (`pnpm test:nuxt -t "AppTimer|TimerEntryRow"`).

## 2. `TimeField` shared component (REQ-361)

- [x] 2.1 Create `apps/web/app/components/TimeField.vue` wrapping `UInputTime` with `hour-cycle="24"`, `granularity="minute"`, `range` passthrough, a `TimeValue | null` (or `{ start, end }`) `v-model`, and passthrough of `size` / `variant` / `ui` / `disabled` / `id` / `aria-label` / `data-testid`; the `#separator` slot renders an optional `separator` prop text. Verify manually in the dev server that a field seeded from a `ZonedDateTime` shows two 24-hour segments and no seconds/AM-PM.
- [x] 2.2 Implement the inline contract in `TimeField`: local draft per side, `commit` emitted on Enter or on `focusout` whose `relatedTarget` is outside the field root, `cancel` on Escape restoring the last committed value, and a `changed` flag (or omitted `commit`) when the draft equals the last committed value. Verify with `apps/web/test/nuxt/shared-ui-components.spec.ts` cases: commit on Enter, commit on focus leaving, no commit when focus moves between segments, Escape cancels, unchanged commit reports unchanged (`pnpm test:nuxt -t "TimeField"`).
- [x] 2.3 Implement the `clampSeconds` prop (default `false`): on commit in range mode, when start and end share a calendar minute and start > end, copy second/millisecond from the untouched bound onto the edited bound before emitting. Verify with component tests for start-edited and end-edited inversions, a non-inverted same-minute pair (untouched), and the prop off (inverted pair emitted as-is).
- [x] 2.4 Verify `TimeField` accessibility with `pnpm lint` (ESLint a11y rules): the root carries the accessible label, segments are keyboard reachable, and no native `<input>` is introduced.

## 3. Timer entry row (REQ-150, REQ-265, REQ-178)

- [x] 3.1 In `TimerEntryRow.vue`, replace the two `w-[10ch]` slots, `startValue` / `stopValue`, `startEditStart` / `startEditStop`, the read-only `UInput` decoys and `combineWithEntryDay` with one fixed-width slot holding `TimeField range` (`size="xs"`, `variant="none"`, `ui.base` = `px-2 py-1 text-sm/4 tabular-nums`, `clampSeconds`, separator = `timerView.entryRow.separator`) bound to `{ start, end }` `ZonedDateTime`s built from the entry in the effective timezone; a running entry renders `TimeField` (single) plus the "now" label in the same slot width; `editingField` narrows to `'title' | null`; `data-testid="timer-entry-times-<id>"` on the field. On `commit`, send one `PATCH` with only the changed `startedAt` / `stoppedAt` converted via `zonedDateTimeToInstant`. Verify manually that a stopped row and a running row align and that editing the minute segment patches the entry.
- [x] 3.2 Rewrite `apps/web/test/nuxt/timer-entry-row.spec.ts` time cases: "normalizes and commits a compact start time" → commits a changed minute with seconds preserved in the PATCH body; "silently reverts an invalid stop time" → unchanged commit sends no request; add same-minute clamp → PATCH carries the clamped instant; keep the same-local-day and stable-slot cases against the new single field (`pnpm test:nuxt -t "TimerEntryRow"`).
- [x] 3.3 Update `apps/web/test/e2e/ui/timer-view-ui.spec.ts` inline-edit journey (around line 467) to use `typeTimeField` on `timer-entry-times-<id>` and add the seconds journey: start and stop a live entry within the same minute, expand the group, retype the stop minute, blur, assert no `PATCH` and no error toast; then type a later minute and assert the row time, group and day totals update. Assert the time slot width is equal for a running row and a stopped row (`pnpm test:e2e:ui -t "timer view"`).

## 4. Manual add-entry dialog (REQ-150)

- [x] 4.1 In `TimerAddEntryDialog.vue`, replace `state.startTime` / `state.endTime` with `state.times: { start: Time | null; end: Time | null }` (defaults 09:00 / 10:00) bound to one `TimeField range` under a single label (`timerView.addEntry.timesLabel`, en/pl, replacing `startLabel` / `endLabel`) with `data-testid="add-entry-time-input"`; build instants at submit from the `CalendarDate` + `Time` in the effective timezone; keep the `rangeError` for end < start and add the incomplete-side error. Verify manually that the form defaults, blocks an inverted or incomplete pair, and creates the entry.
- [x] 4.2 Update `apps/web/test/nuxt/timer-add-entry-dialog.spec.ts`: drive `{ start, end }` through the `UInputTime` instance's `update:modelValue`, assert the POST payload instants, the inverted-pair error, and the incomplete-side error (`pnpm test:nuxt -t "add-entry"`).
- [x] 4.3 Update the add-entry journeys in `apps/web/test/e2e/ui/timer-view-ui.spec.ts` (around lines 172–203) to fill the range with `typeTimeField(page, 'add-entry-time-input', '09:00', 0)` / `('10:00', 1)` and assert the created entry's start–stop in the row (`pnpm test:e2e:ui -t "add entry"`).
- [x] 4.4 Verify `pnpm test:unit -t "i18n-catalog-parity"` passes after replacing the start/end labels with the range label in both catalogs.

## 5. Running-start popover (REQ-146)

- [x] 5.1 In `AppTimer.vue`, replace `startTime: string | null` with `startTime: ShallowRef<Time | null>` seeded from the running entry's zoned start, render `TimeField` (single, default size) with the existing `id` / `data-testid="timer-start-editor-time-input"` / `aria-label`, disable save while `startDate` or `startTime` is `null`, and compose `combineStartedAt()` from `CalendarDate` + `Time` in the effective timezone. Verify manually that typing `9 0 0` fills `09:00`, an incomplete time disables save, and saving rebases the ticker.
- [x] 5.2 Update `apps/web/test/nuxt/AppTimer.spec.ts` start-editor cases: seed the time via the `UInputTime` stub, assert the PATCH `startedAt`, assert save is disabled when the time is `null`, keep the future-start inline-error case (`pnpm test:nuxt -t "AppTimer"`).
- [x] 5.3 Update `apps/web/test/e2e/ui/timer-topbar-start-edit.spec.ts` to fill the time with `typeTimeField` instead of `fill` on the old input (three sites around lines 47, 103, 162) and keep the existing assertions (`pnpm test:e2e:ui -t "start edit"`).

## 6. Retire the smart time input (REQ-131 → REQ-362)

- [x] 6.1 Rename `TimeInput.vue` to `DurationInput.vue`, remove the `duration` prop and the clock branch so it always normalizes `HH:MM:SS`, update `SyncDayRow.vue` to import it (behaviour unchanged), and delete `apps/web/app/utils/normalize-time-input.ts` with `apps/web/test/unit/normalize-time-input.spec.ts`. Verify `pnpm lint` reports no dangling imports and `pnpm test:unit -t "normalize-duration-input"` still passes.
- [x] 6.2 Move the `TimeInput` block in `apps/web/test/nuxt/shared-ui-components.spec.ts` to `DurationInput` (bare minutes, `H:MM`, unbounded hours such as `26:15`, invalid reverts, Escape) and confirm `apps/web/test/nuxt/remote-sync-page.spec.ts` and `apps/web/test/e2e/ui/remote-sync-ui.spec.ts` to-send cases pass unchanged (`pnpm test:nuxt -t "DurationInput|remote-sync"`, `pnpm test:e2e:ui -t "remote-sync"`).
- [x] 6.3 Confirm no `TimeInput` or `normalizeTimeInput` reference remains under `apps/web` (`grep -rn "TimeInput\|normalizeTimeInput" apps/web/app apps/web/test` returns only `DurationInput`), and that every clock-time site renders `TimeField` (`grep -rln "<TimeField" apps/web/app/components` lists `TimerEntryRow`, `TimerAddEntryDialog`, `AppTimer`).

## 7. Docs and final verification

- [x] 7.1 Extend the UI bullet in `AGENTS.md` (Code Style → UI) to name `UInputTime` via `TimeField` for clock times (single and `range`) alongside the existing `UInputDate` sentence, and verify the wording matches REQ-361.
- [x] 7.2 Run `pnpm lint`, `pnpm format:check`, `pnpm type-check`, `pnpm test:unit`, `pnpm test:nuxt`, and `pnpm test:e2e:ui` and verify all pass.
