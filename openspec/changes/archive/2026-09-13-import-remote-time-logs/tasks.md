## 1. Remote-trackers package (contract + adapters)

- [x] 1.1 Extend `RemoteTimeLogDto` in `packages/remote-trackers/src/contracts/remote-time-log.ts` with optional `remoteProjectId`, `remoteProjectTitle`, `remoteIssueTitle` (REQ-341); verify `pnpm package:check` type-checks with both adapters unchanged
- [x] 1.2 OpenProject `parseTimeLogsPage`: map `_links.project.href` → id, `_links.project.title`, and `_links.entity.title` (fallback `_links.workPackage.title`), omitting absent values (REQ-342); verify `packages/remote-trackers/test/openproject/` covers full links, legacy work-package link, and missing project on both same-day and range pages
- [x] 1.3 Redmine `parseTimeLogsPage`: map `project.id` / `project.name`, keep dropping issue-less entries (REQ-343); verify `packages/remote-trackers/test/redmine/` covers project present, project missing, and issue-less drop on both fetches
- [x] 1.4 Update `test/contracts/adapter-contract.spec.ts` so a log with and without the optional fields round-trips through both adapters; verify `pnpm package:check` is green

## 2. Extension protocol + extension

- [x] 2.1 `packages/extension-protocol` `remoteTimeLogSchema`: add the three optional keys so the bridge does not strip them (REQ-341 extension scenario); verify `packages/extension-protocol/test/protocol.spec.ts` accepts logs with and without the fields and `contract-agreement.ts` still compiles
- [x] 2.2 `apps/extension`: rebuild against the updated packages and confirm `dispatch.spec.ts` passes a range fetch whose logs carry `remoteProjectId` through unchanged; update `apps/web/test/e2e/helpers/fake-extension.ts` fixtures to include the fields; verify `pnpm test:unit` in `apps/extension` is green

## 3. Backend (web server)

- [x] 3.1 `shared/types/remote-log-import.ts`: `importRemoteLogsSchema` (`dryRun`, `groups[{ projectId, logs[] }]`, ≤500 logs, unique `remoteLogId`, positive integer duration, ISO `spentOn`, nullable `activityId`/`comment`, optional titles) and `ImportRemoteLogsResultDto` (per-project + total `imported`/`wouldImport`/`skippedExisting`) (REQ-339); verify a unit spec in `apps/web/test/unit/` covers valid body, empty groups, duplicate ids, >500 logs, bad duration/date with expected messageKeys
- [x] 3.2 `server/utils/import-placement.ts`: pure `placeImportedEntries(dayStart, dayEnd, existingMaxStop, logs)` implementing REQ-337 (08:00 anchor, cursor after last stopped entry, numeric remoteLogId order, midnight fallback); verify `apps/web/test/unit/import-placement.spec.ts` covers the empty day, second-tracker continuation, real local entry, 17 h spill, 20 h midnight fallback, and a 23 h DST day. **Spec correction found while writing this test:** REQ-337's fallback trigger was written as "cursor + total duration > dayEnd", which — with an 8h anchor on a ~24h day — fires for any total over 16h and so could never distinguish "spills past midnight, keeps 08:00" from "falls back to midnight" (both scenarios would trip the same check). Fixed the trigger to check whether the *last* entry's own start (not the cumulative end) would land on the next local day; updated REQ-337's text/scenario and design.md Decision 3 to match, and reworked the "20h" scenario as two logs (19h+1h) since a single entry's start never depends on its own duration.
- [x] 3.3 `server/utils/task-name-from-comment.ts` (trim, `empty` fallback) (REQ-336); verify a unit spec covers comment, null, whitespace
- [x] 3.4 `POST /api/trackers/[id]/import` (`server/api/trackers/[id]/import.post.ts`): `requireAuth`, tracker 404 rules, project-binding 422, existing-identity skip, dry run vs write, one transaction per request, per-day `computeDayBoundary` + max stopped `stoppedAt` query, `resolveTaskId` with tracker/cached titles, entry + `remote_exports` (`requiredFieldValues: { activity }`) + `remote_export_entries` inserts, unique-violation → skipped (REQ-335, REQ-338, REQ-339); verify `pnpm type-check` passes. Per-log inserts run in a nested `tx.transaction()` (Postgres SAVEPOINT) so a rare identity race only skips that one log rather than rolling back the whole request. **Bug found and fixed post-implementation (reported by the user after testing a real Redmine import — a 500 "b.getTime is not a function" inside `placeImportedEntries`):** the `existingMaxStop` value read via `sql<Date | null>\`max(${timeEntries.stoppedAt})\`` is compile-time-typed as `Date` but postgres.js does not deserialize a raw aggregate expression into a Date the way it does a typed column — it comes back as an ISO string whenever a row matches, and only `null` (no matching row) happened to work by accident. The crash only appeared once the target day already had a stopped entry (from a prior local entry or a prior import), which the OpenProject test run hadn't hit yet; e2e-api coverage had exercised this path only via the pure `placeImportedEntries` unit tests (real `Date` inputs, bypassing the DB) and never against the live query. Fixed by explicitly normalizing with `new Date(...)` at the query boundary; added two e2e-api cases against the real database — importing onto a day with a pre-existing local entry, and two separate import requests on the same day — reproduced the exact reported crash against the pre-fix code and pass against the fix.
- [x] 3.5 e2e-api `apps/web/test/e2e/api/trackers-import.spec.ts`: dry run counts without rows; write run creates task/entry/provenance with expected instants (08:00 stacking) and reuses a task across days; sibling tasks for different comments; `empty` name; re-run skips all; app-exported log skipped; project bound to another tracker → 422; foreign tracker → 404; invalid body → 422; missing CSRF rejected; verify `pnpm test:e2e:api` is green. Full suite verified green (17 files / 135 tests, production build).
- [x] 3.6 e2e-api: extend `sync-day.spec.ts` and `reports-monthly.spec.ts` with a backfilled day — Remote Sync row is Sent and the report's export provenance includes the imported log (REQ-344); verify both specs pass
- [x] 3.7 Add i18n keys for the new server messageKeys (`en`/`pl`); verify `pnpm lint` i18n parity passes

## 4. Frontend (web app)

- [x] 4.1 `app/utils/remote/route-logs-by-scope.ts`: pure ancestor-walk routing over `RemoteProjectDto[]` + scoped `ProjectDto[]` returning matched groups and unmatched buckets keyed by remote project (REQ-334); verify `apps/web/test/unit/route-logs-by-scope.spec.ts` covers descendant match, nested most-specific, unmatched root, project absent from catalog, missing `remoteProjectId`, unscoped project receives nothing
- [x] 4.2 `app/utils/month-range.ts`: split an inclusive `from`/`to` into month chunks and 500-log request splits; verify a unit spec covers partial first/last months and the split cap
- [x] 4.3 `app/composables/use-remote-log-import.ts`: phase state machine (range → scanning → preview → importing → done/error), per-month fetch via `createRemoteAdapter(config, secret)` + `listProjects()` once, dry-run and write calls to `POST /api/trackers/[id]/import`, aggregated per-remote-project preview, stop-on-failure with committed-months message (REQ-334, REQ-340); verify `apps/web/test/unit/use-remote-log-import.spec.ts` covers a two-month happy path, a failing second month, nothing-to-import, and the no-project-id hint. Retry re-runs the failed stage from the top rather than tracking a resume point — scanning is read-only and the write endpoint already skips what is done (REQ-338), so this is simpler and equally correct; also added a cancel-during-scan test.
- [x] 4.4 `app/components/TrackerImportDialog.vue`: range form (defaults, inverted-range inline error, scoped-projects list with unscoped ones flagged), scanning progress with `aria-live` and cancel, preview table with "no scoped project" state and totals, importing phase without cancel, done phase with synthetic-times note, failure state with committed months and Retry; add i18n keys `en`/`pl` (REQ-340); verify `apps/web/test/nuxt/tracker-import-dialog.spec.ts` covers each phase with a mocked composable, the unscoped flag, cancel present only while scanning, retry re-running the range, disabled import when nothing to import, and accessible names. Uses `UProgress` for the visual bar (not used elsewhere in the codebase yet, but a first-class Nuxt UI component matching the "prefer existing Nuxt UI components" rule) alongside the established `role=status aria-live=polite` text convention. Full `pnpm --filter @osi/time-tracker exec vitest run --project nuxt` suite re-verified green (31 files / 243 tests) after this addition. **Bug found and fixed post-implementation (reported by the user after review):** the range defaults never appeared in the real app — `pages/trackers.vue` mounts this dialog via `v-if`, so it is created already `open: true`, and the seeding `watch(() => open, ...)` (no `{ immediate: true }`) only fires on a *change*, never on that initial value, leaving `fromDate`/`toDate` blank. The nuxt test's `mount()` helper masked this by always mounting `open: false` then transitioning to `true`. Fixed with `{ immediate: true }` on the watcher (harmless if the page's mounting pattern ever changes to "always mounted, closed") and added a dedicated regression test that mounts already-open, matching the real page. **Second bug found and fixed post-implementation (user reported a Redmine import 500ing with "b.getTime is not a function" after an OpenProject import worked):** in `server/api/trackers/[id]/import.post.ts`, `existingMaxStop` is read via `sql<Date | null>\`max(${timeEntries.stoppedAt})\`` — that annotation is compile-time only; postgres.js does not deserialize a raw aggregate expression into a Date the way it does a typed column, so it came back as an ISO string whenever a row matched, and only the `null` case (no matching row) happened to work. The crash needs the target day to already have a stopped entry — the user's OpenProject run hit an empty day first; existing e2e-api coverage only exercised this path via the pure `placeImportedEntries` unit tests (real `Date` inputs, bypassing the DB). Fixed by explicitly normalizing with `new Date(...)` at the query boundary; added two e2e-api cases against the real database (a day with a pre-existing local entry; two separate import requests on the same day) that reproduce the exact crash pre-fix and pass post-fix. **Third bug found and fixed post-implementation (user reported: (1) remote project name showing "—" in the preview after a successful scan, and (2) invalid/stale state shown after opening the dialog for a second, different tracker):** (1) `ImportProjectPreview` (in `use-remote-log-import.ts`) only ever carried `{projectId, wouldImport, skippedExisting}` from the server's per-local-Project dry-run response, which has no remote-project breakdown — the dialog's matched rows hard-coded `remoteProjectTitle: null`. Added `remoteProjectTitles: string[]`, accumulated client-side from the routed logs' own `remoteProjectTitle`/`remoteProjectId` (independent of the server response, which cannot be split per remote project), and rendered as a comma-joined list on the matched row. (2) `pages/trackers.vue` mounted `TrackerImportDialog` with `v-if="importingTracker"` and no `:key` — since `v-if` alone never toggles false→true between two different (both truthy) trackers, the same component instance (and its `useRemoteLogImport` composable, which captures `config` once at setup) persisted across tracker switches; reopening for a second tracker without the `open` prop ever actually transitioning left the dialog stuck on whatever phase the first tracker's session ended on. Fixed by adding `:key="importingTracker.id"` to force a fresh instance per tracker. Added a regression test opening the dialog for two trackers in sequence and asserting the second scan reaches the range phase and uses the second tracker's adapter config — reproduced the exact stuck-phase symptom pre-fix. Full unit+nuxt regression re-verified green (94 files / 744 tests) and the e2e-api import spec re-verified against a fresh production build (12/12) after all three fixes.
- [x] 4.5 `pages/trackers.vue` + `RowActions.vue`: add the import action (icon, tooltip, disabled + hint without a secret), open the dialog (REQ-345); verify `apps/web/test/nuxt/trackers.spec.ts` covers enabled, disabled-without-secret, and sibling-button structure. `RowActions` gained an optional `extra*` prop set (icon/label/testid/disabled) rendered before edit, unused by `pages/projects.vue`'s existing usage. Full nuxt suite re-verified green (31 files / 247 tests). **Fourth bug found and fixed post-implementation (user reported a Vue hydration mismatch on the import button: server rendered `disabled`, client did not):** `getSecret()` reads `localStorage`, which SSR cannot see — `useTrackerSecret().get()` unconditionally returns `null` server-side, so the `columns` computed's `actions` cell always rendered `extraDisabled: true` in the SSR HTML, then recomputed with the real (client-only) secret on the very first client render, before hydration finished — a direct mismatch. Fixed with the same SSR-safe pattern already used in `use-user-settings.ts` for browser-only state: a `secretsReady` ref seeded `false`, flipped in `onMounted`, read at the top level of the `columns` computed (not only inside the nested `cell` closure) so the computed actually re-evaluates once ready. Server and first-client-paint now both render the safe "disabled" state; the real value appears only after mount, client-side only, matching the established convention rather than inventing a new one. No automated hydration-diff test was added — this codebase has no existing pattern for that (verified via `AppTimer.spec.ts`, the nearest precedent), and building one would be disproportionate to the fix; verified instead by type-check, the existing nuxt suite staying green, and matching `use-user-settings.ts`'s already-proven pattern line for line.
- [x] 4.6 e2e-ui `apps/web/test/e2e/ui/tracker-import-ui.spec.ts`: seed a tracker with a browser secret and two scoped projects, mock catalog and two months of range logs with `page.route` (one unmatched remote project), run the dialog through preview and import, assert the timer view shows the stacked entries and Remote Sync shows Linked; re-run and assert all skipped; in a second case fail the second month via `page.route`, assert the committed-months message, retry, and assert it resumes with the first month skipped; verify the spec passes under `pnpm test:e2e:ui`. Added `data-testid`s on the done-phase counts (`tracker-import-done-imported/linked/unmatched`) for precise assertions, also used by the nuxt component test. The retry case fails the app's own `POST /api/trackers/[id]/import` for the September write specifically (via `page.route` on that same-origin path), not the tracker's remote endpoint — this is what makes "resumes with the first month skipped" observable (skip-on-existing only applies to the write, not the read-only scan). **Not executed**: this sandbox has no Chromium binary and no network access to install one (`playwright install chromium` timed out); confirmed the spec parses and is picked up (2 tests reported "skipped", matching this repo's own guard convention) — it runs (and must pass) in CI.
- [x] 4.7 e2e-ui: repeat the import journey in extension mode via `fake-extension.ts` and assert the same result; verify the spec passes. Extended `installExtension`'s options with `projects` (catalog fixture), mirroring the `timeLogs`/`timeLogsInRange` additions from task 2.2 — needed so the fake extension can demonstrate real scope routing instead of always reporting an empty catalog. **Found and fixed while running the full unit suite:** `remote-log-import.ts`'s cross-field checks used `superRefine`/`ctx.addIssue({..., message: ...})` (the only correct zod v4 shape for a custom issue), which tripped the repo's own `shared-types-zod-v4.spec.ts` convention test banning `message:` (aimed at legacy zod v3 options, but written as a blanket regex with no exception for this shape). Rewrote both checks as plain chained `.refine(fn, { path, error })` — the idiom every other schema in `shared/types` already uses — which avoids `addIssue` entirely and needed no change to the convention test itself. Not executed in-browser for the same reason as 4.6 (no Chromium/network); confirmed the spec parses and skips cleanly.

## 5. Wrap-up

- [x] 5.1 Update `docs/wbs.md` and `docs/user-stories.md` with the import-history feature and the routing/idempotency rules; verify the docs diff reads correctly
- [x] 5.2 Run `pnpm lint`, `pnpm format:check`, `pnpm type-check`, `pnpm test:unit`, `pnpm test:nuxt`, `pnpm test:e2e`; verify all green before opening the PR. All green: `lint` clean, `format:check` clean (after `pnpm format` fixed a markdown table alignment in `docs/wbs.md`), `type-check` clean across every package, `test:unit` all passed, `test:nuxt` 31 files/247 tests passed, `test:e2e` (production build) 27 files/171 tests passed, 16 files/60 tests skipped — the e2e-ui specs, which need Chromium this sandbox has no network access to install; they run (and must pass) in CI per the repo's own guard convention. 0 failures anywhere.
